<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <IsPackable>true</IsPackable>
    <PackageId>VelloSharp.TreeDataGrid</PackageId>
    <Title>VelloSharp.TreeDataGrid</Title>
    <Description>Tree-structured data models, render loops, and native interop for VelloSharp dashboards.</Description>
    <PackageTags>vello;treedatagrid;visualization</PackageTags>
  </PropertyGroup>

  <PropertyGroup>
    <VelloUseNativePackageDependencies Condition="'$(VelloUseNativePackageDependencies)'==''">false</VelloUseNativePackageDependencies>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\VelloSharp.Composition\VelloSharp.Composition.csproj" />
    <ProjectReference Include="..\VelloSharp.ChartDiagnostics\VelloSharp.ChartDiagnostics.csproj" />
  </ItemGroup>

  <ItemGroup Condition="'$(VelloUseNativePackageDependencies)'=='true'">
    <PackageReference Include="VelloSharp.Native.TreeDataGrid.android-arm64"
                      Condition="Exists('$(RepositoryRoot)/artifacts/runtimes/android-arm64/native')" />
    <PackageReference Include="VelloSharp.Native.TreeDataGrid.browser-wasm"
                      Condition="Exists('$(RepositoryRoot)/artifacts/runtimes/browser-wasm/native')" />
    <PackageReference Include="VelloSharp.Native.TreeDataGrid.ios-arm64"
                      Condition="Exists('$(RepositoryRoot)/artifacts/runtimes/ios-arm64/native')" />
    <PackageReference Include="VelloSharp.Native.TreeDataGrid.iossimulator-x64"
                      Condition="Exists('$(RepositoryRoot)/artifacts/runtimes/iossimulator-x64/native')" />
    <PackageReference Include="VelloSharp.Native.TreeDataGrid.linux-arm64"
                      Condition="Exists('$(RepositoryRoot)/artifacts/runtimes/linux-arm64/native')" />
    <PackageReference Include="VelloSharp.Native.TreeDataGrid.linux-x64"
                      Condition="Exists('$(RepositoryRoot)/artifacts/runtimes/linux-x64/native')" />
    <PackageReference Include="VelloSharp.Native.TreeDataGrid.osx-arm64"
                      Condition="Exists('$(RepositoryRoot)/artifacts/runtimes/osx-arm64/native')" />
    <PackageReference Include="VelloSharp.Native.TreeDataGrid.osx-x64"
                      Condition="Exists('$(RepositoryRoot)/artifacts/runtimes/osx-x64/native')" />
    <PackageReference Include="VelloSharp.Native.TreeDataGrid.win-arm64"
                      Condition="Exists('$(RepositoryRoot)/artifacts/runtimes/win-arm64/native')" />
    <PackageReference Include="VelloSharp.Native.TreeDataGrid.win-x64"
                      Condition="Exists('$(RepositoryRoot)/artifacts/runtimes/win-x64/native')" />
  </ItemGroup>

  <PropertyGroup>
    <RepositoryRoot>$([System.IO.Path]::GetFullPath('$(MSBuildThisFileDirectory)..\..'))</RepositoryRoot>
    <_CargoProfileFlag Condition="'$(Configuration)'=='Debug'"></_CargoProfileFlag>
    <_CargoProfileFlag Condition="'$(Configuration)'!='Debug'">--release</_CargoProfileFlag>
    <_CargoProfileDir Condition="'$(Configuration)'=='Debug'">debug</_CargoProfileDir>
    <_CargoProfileDir Condition="'$(Configuration)'!='Debug'">release</_CargoProfileDir>
  </PropertyGroup>

  <Target Name="BuildTreeDataGridNative"
          BeforeTargets="Build"
          Condition="'$(DesignTimeBuild)'!='true' and '$(VelloSkipNativeBuild)'!='true' and '$(VelloUseNativePackageDependencies)'!='true'">
    <Exec Command="cargo build -p vello_tree_datagrid $(_CargoProfileFlag)"
          WorkingDirectory="$(RepositoryRoot)" />
  </Target>

  <Target Name="CopyTreeDataGridNative"
          AfterTargets="Build"
          Condition="'$(DesignTimeBuild)'!='true' and '$(VelloSkipNativeBuild)'!='true' and '$(VelloUseNativePackageDependencies)'!='true'">
    <ItemGroup>
      <_TreeNative Include="$(RepositoryRoot)\target\$(_CargoProfileDir)\vello_tree_datagrid.dll"
                   Condition="Exists('$(RepositoryRoot)\target\$(_CargoProfileDir)\vello_tree_datagrid.dll')" />
      <_TreeNative Include="$(RepositoryRoot)\target\$(_CargoProfileDir)\libvello_tree_datagrid.so"
                   Condition="Exists('$(RepositoryRoot)\target\$(_CargoProfileDir)\libvello_tree_datagrid.so')" />
      <_TreeNative Include="$(RepositoryRoot)\target\$(_CargoProfileDir)\libvello_tree_datagrid.dylib"
                   Condition="Exists('$(RepositoryRoot)\target\$(_CargoProfileDir)\libvello_tree_datagrid.dylib')" />
    </ItemGroup>

    <Copy SourceFiles="@(_TreeNative)"
          DestinationFolder="$(OutDir)"
          SkipUnchangedFiles="true" />
    <Copy SourceFiles="@(_TreeNative)"
          DestinationFolder="$(OutDir)runtimes\$(RuntimeIdentifier)\native"
          Condition="'$(RuntimeIdentifier)'!=''"
          SkipUnchangedFiles="true" />
    <Copy SourceFiles="@(_TreeNative)"
          DestinationFolder="$(OutDir)runtimes\win-x64\native"
          Condition="'$(RuntimeIdentifier)'=='' and Exists('$(OutDir)')"
          SkipUnchangedFiles="true" />
  </Target>
</Project>
